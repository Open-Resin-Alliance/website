{
  "name": "openresin-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "openresin.org — the Open Resin Alliance website",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "stats": "node scripts/fetch-github-stats.mjs && node scripts/fetch-open-collective.mjs",
    "stats:offline": "node scripts/fetch-github-stats.mjs --offline && node scripts/fetch-open-collective.mjs --offline"
  },
  "dependencies": {
    "@astrojs/mdx": "^8.0.1",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}
