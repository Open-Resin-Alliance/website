# Store text files with LF line endings and check them out with LF on every platform.
# The specification snapshots under src/content/specs/ are generated by
# scripts/sync-specs.mjs from other repositories. Without this, a checkout on any
# platform other than the one that generated them rewrites every page and buries
# real changes in line-ending noise.
* text=auto eol=lf
